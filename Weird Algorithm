#include <bits/stdc++.h>
using namespace std;
#define endl "\n"
#define int long long
 
signed main() 
{
	int tests=1; //cin>>tests;
	while(tttt--)
	{
		int n; cin>>n;
		while(1)
		{
			cout<<n<<' ';
			if(n == 1) break;
			if(n & 1) n = 3 * n + 1;
			else n/=2; 
		}
	}
	return 0;
}
